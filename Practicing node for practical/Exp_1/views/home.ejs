<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .home-container {
            width: 400px;
            margin: 0 auto;
            padding: 40px 30px;
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 8px 24px rgba(44, 62, 80, 0.13);
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 60px;
        }
        .welcome {
            font-size: 2rem;
            font-weight: 700;
            color: #2d3e50;
            margin-bottom: 18px;
        }
        .desc {
            color: #636e72;
            font-size: 1.1rem;
            margin-bottom: 28px;
            text-align: center;
        }
        .logout-btn {
            padding: 10px 24px;
            background: linear-gradient(90deg, #74ebd5 0%, #ACB6E5 100%);
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.2s;
        }
        .logout-btn:hover {
            background: linear-gradient(90deg, #ACB6E5 0%, #74ebd5 100%);
        }
    </style>
</head>
<body>
    <div class="home-container">
        <div class="welcome">Welcome!</div>
        <div class="desc">
            You have successfully logged in.<br>
            Explore the features of this demo home page.<br>
            Thank you for visiting!
        </div>
        <form action="/logout" method="post">
            <button class="logout-btn" type="submit"><a href="/"></a>Logout</button>
        </form>
    </div>
</body>
</html>